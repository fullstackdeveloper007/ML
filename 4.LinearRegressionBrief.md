## Mean Square Error (Cost Function) 

![image](https://github.com/user-attachments/assets/bc089016-f1a0-451d-9b2a-49af2b6afb5e)

Where:
- ŷᵢ = m * xᵢ + b (predicted value)
- yᵢ = actual value
- m = slope (weight)
- b = intercept (bias)
- xᵢ = input feature
### Error = (ŷᵢ - yᵢ)

## 🧮 Partial Derivatives (Gradients)

To minimize the cost function, we take the partial derivatives with respect to **m** and **b**.

### ➤ Derivative w.r.t m (slope):

```
∂J/∂m = (2/m) * Σ( (ŷᵢ - yᵢ) * xᵢ )
```

- This tells how much the slope affects the cost.

---

### ➤ Derivative w.r.t b (intercept):

```
∂J/∂b = (2/m) * Σ( (ŷᵢ - yᵢ) )
```

- This tells how much the intercept affects the cost.

---

## ✅ Implementation Logic

Let:
```
errorᵢ = ŷᵢ - yᵢ
```

Then:
- `dm = (2/m) * Σ(errorᵢ * xᵢ)`
- `db = (2/m) * Σ(errorᵢ)`

---

## 🧠 Intuition

These gradients are used in gradient descent to update `m` and `b`:

```python
m = m - learning_rate * dm
b = b - learning_rate * db
```

---

## ✏️ Optional Variant

Some implementations simplify by removing the factor of `2`:

- `∂J/∂m = (1/m) * Σ((ŷᵢ - yᵢ) * xᵢ)`
- `∂J/∂b = (1/m) * Σ(ŷᵢ - yᵢ)`

Both forms are correct — the difference is only in scaling.

